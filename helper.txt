import type { Config } from 'tailwindcss'

export default {
  content: [
    "./app/**/*.{js,ts,jsx,tsx}",
    "./components/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        sans: ['var(--font-inter)', 'sans-serif'],
        inter: ['var(--font-inter)', 'sans-serif'],
      },
      
      // 1. COLORS (Accepts hex, rgb, rgba, hsl)
      colors: {
        brand: {
          primary: '#3b82f6',
          secondary: '#10b981',
          vibrant: '#ff007f', // Custom naming
        },
        dark: '#121212',
      },

      // 2. SPACING (Applies globally to padding, margin, gap, top, left, etc.)
      spacing: {
        'xs-tight': '0.25rem',  // 4px
        'md-cozy': '1.5rem',    // 24px
        '128': '32rem',         // 512px
      },

      // 3. FONT SIZES (Accepts a string, or an array with [fontSize, lineHeight])
      fontSize: {
        'micro': '0.65rem',
        'hero': ['4.5rem', { lineHeight: '1.1', letterSpacing: '-0.02em' }],
      },

      // 4. FONT WEIGHTS (Maps directly to CSS font-weight numbers or strings)
      fontWeight: {
        hairline: '100',
        'ultra-bold': '900',
      },

      // 5. OPACITY (Values between 0 and 1, or percentages as strings)
      opacity: {
        'ghost': '0.08',       // 8% visible
        'semi-clear': '0.45',  // 45% visible
      },

      // 6. WIDTHS (Applies strictly to width classes. Spacing values automatically work for width, but add here for width-only specials)
      width: {
        'chunky-card': '450px',
        'readable-prose': '65ch',
      }

    },
  },
  plugins: [],
} satisfies Config


// app/manifest.ts
import { MetadataRoute } from 'next'

export default function manifest(): MetadataRoute.Manifest {
  return {
    name: "FudStack - The Chef's Key",
    short_name: 'FudStack',
    description: 'An AI powered multi-tenant e-commerce app making workflows fast and reliable.',
    start_url: '/',
    display: 'standalone', // Hides browser address bar
    background_color: '#ffffff', // Splash screen background
    theme_color: '#111827', // Colors the mobile top status bar
    icons: [
      {
        src: '/icon.png',
        sizes: '512x512',
        type: 'image/png',
        purpose: 'any', 
      },
      {
        src: '/icon.png',
        sizes: '512x512',
        type: 'image/png',
        purpose: 'maskable', // Ensures Android won't cut off your logo edges
      },
    ],
  }
}




"use client";





export default function FAQ() {


  return (
    <section className="relative overflow-hidden bg-black py-28">
      {/* Background Blur */}
      <div className="absolute left-1/2 top-20 h-96 w-96 -translate-x-1/2 rounded-full bg-orange-500/10 blur-[150px]" />

      <div className="relative mx-auto max-w-6xl px-6">
        <motion.div
         
          className="mb-20 text-center"
        >
          <p className="mb-4 uppercase tracking-[0.45em] text-orange-400 text-sm">
            Frequently Asked Questions
          </p>

          <h2 className="mx-auto max-w-3xl text-4xl font-semibold tracking-tight text-white md:text-6xl">
            Everything You Need To Know
          </h2>

          <p className="mx-auto mt-6 max-w-2xl text-base leading-8 tracking-wide text-neutral-400">
            Find answers to the most common questions about our restaurant
            management platform.
          </p>
        </motion.div>

        <div className="space-y-5">
        
        </div>
      </div>
    </section>
  );
}

//gsap timeline if you want to control it outside the useGsap hook
export default function ControlledTimeline() {
  const container = useRef<HTMLDivElement>(null);
  const tl = useRef<gsap.core.Timeline | null>(null); // Store timeline reference

  useGSAP(() => {
    // Assign timeline to the ref so it can be accessed globally in this component
    tl.current = gsap.timeline({ paused: true })
      .to(".box", { x: 100, rotation: 180 });
  }, { scope: container });

  return (
    <div ref={container}>
      <div className="box w-16 h-16 bg-green-500"></div>
      
      {/* Control the timeline on click */}
      <button onClick={() => tl.current?.play()}>Play</button>
      <button onClick={() => tl.current?.reverse()}>Reverse</button>
    </div>
  );
}

\
{open && (
  <Modal>
    {step === "select" && (
      <SelectClient
        setStep={setStep}
      />
    )}

    {step === "clientTerms" && (
      <ClientTerms />
    )}

    {step === "agentTerms" && (
      <AgentTerms />
    )}
  </Modal>
)}




"use client"
import logo from "@/public/icon.png" // To be worked on..
import Image from "next/image"
import { brand } from "@/brand"
import facbookLogo from "@/public/facebook.svg"
import goggleLogo from "@/public/google.svg"
function SignUpAgent() {
  return (
    <>
    <main>
     <div>
      <Image src={logo} alt="FudStack Logo" aria-label="FudStack logo" width={24} height={24} className="m-4"/>
     </div>
    <section
    className="
    fixed inset-0
    flex justify-center items-center
    "
    >
       <div
     className="bg-primary-bone h-auto max-h-[90dvh] w-auto max-w-178 shadow-lg tracking-tighter"
     >
     {/*  <div>
      <Image src={logo} alt={brand.ariaLogo} aria-label={brand.ariaLogo} width={24} height={24} className="m-4"/>
     </div> */}
      <section
      className="flex"  /* Main container */
      >
       <div className="w-auto max-w-128">
        
         <div className="m-5 flex flex-col gap-0.5">
          <h2 className=" leading-none tracking-tighter text-md md:text-xl">Get Started Now</h2>
          <p className="tracking-tighter leading-none text-xs">Enter your credentials to access your account</p>
         </div>



/* eslint-disable @next/next/no-img-element */
"use client";

import React, { SetStateAction, useState } from "react";
import { motion, AnimatePresence } from "framer-motion";
import { HiOutlineGlobeAlt, HiOutlineMap, HiOutlineOfficeBuilding, HiOutlineClock } from "react-icons/hi";
import { WorldGlobe } from "./WorldGlobe"; // Dynamic Aceternity Globe implementation wrapper

interface Props {
  setStep: React.Dispatch<SetStateAction<number>>;
}

interface LocationFormState {
  country: string;
  state: string;
  city: string;
  address: string;
  businessHours: string;
}

function LocationConfig({ setStep }: Props) {
  const [formData, setFormData] = useState<LocationFormState>({
    country: "",
    state: "",
    city: "",
    address: "",
    businessHours: "",
  });

  const handleInputChange = (e: React.ChangeEvent<HTMLInputElement | HTMLTextAreaElement>) => {
    const { id, value } = e.target;
    setFormData((prev) => ({ ...prev, [id]: value }));
  };

  const handleSubmit = (e: React.FormEvent) => {
    e.preventDefault();
    setStep((prev) => prev + 1);
  };

  return (
    <motion.section
      initial={{ opacity: 0, height: 120, filter: "blur(4px)" }}
      animate={{ opacity: 1, height: "auto", filter: "blur(0px)" }}
      transition={{ duration: 0.45, ease: "easeInOut" }}
      exit={{ opacity: 0, height: 120, filter: "blur(4px)" }}
      className="w-full flex justify-center items-center p-4"
    >
      <div className="grid grid-cols-1 lg:grid-cols-12 max-w-4xl w-full bg-white rounded-2xl shadow-md border border-neutral-100 overflow-hidden min-h-[540px]">
        
        {/* Left Form Column */}
        <div className="lg:col-span-7 flex flex-col justify-between p-6 md:p-8">
          <div>
            {/* Header */}
            <div className="space-y-1.5 mb-6">
              <h1 className="text-xl font-bold tracking-tight text-neutral-900">
                Setup Your Location
              </h1>
              <p className="text-xs text-neutral-500 leading-relaxed max-w-md">
                Setting up your operational location enables us to optimize service deliveries and map your customer radius accurately.
              </p>
            </div>

            {/* Form Fields */}
            <form onSubmit={handleSubmit} className="space-y-4">
              
              {/* Country & State Grid */}
              <div className="grid grid-cols-2 gap-4">
                <div className="space-y-1">
                  <label htmlFor="country" className="block text-xs font-medium text-neutral-700">
                    Country
                  </label>
                  <div className="flex px-3 py-2 items-center border rounded-lg border-neutral-200 bg-white focus-within:border-orange-500 focus-within:ring-2 focus-within:ring-orange-500/10 transition-all duration-150">
                    <HiOutlineGlobeAlt className="w-4 h-4 text-neutral-400 shrink-0" />
                    <input
                      type="text"
                      id="country"
                      value={formData.country}
                      onChange={handleInputChange}
                      placeholder="e.g. United States"
                      className="flex-1 ml-2.5 outline-none bg-transparent text-xs text-neutral-900 placeholder:text-neutral-400"
                      required
                    />
                  </div>
                </div>

                <div className="space-y-1">
                  <label htmlFor="state" className="block text-xs font-medium text-neutral-700">
                    State / Region
                  </label>
                  <div className="flex px-3 py-2 items-center border rounded-lg border-neutral-200 bg-white focus-within:border-orange-500 focus-within:ring-2 focus-within:ring-orange-500/10 transition-all duration-150">
                    <HiOutlineMap className="w-4 h-4 text-neutral-400 shrink-0" />
                    <input
                      type="text"
                      id="state"
                      value={formData.state}
                      onChange={handleInputChange}
                      placeholder="e.g. California"
                      className="flex-1 ml-2.5 outline-none bg-transparent text-xs text-neutral-900 placeholder:text-neutral-400"
                      required
                    />
                  </div>
                </div>
              </div>

              {/* City & Address Grid */}
              <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
                <div className="md:col-span-1 space-y-1">
                  <label htmlFor="city" className="block text-xs font-medium text-neutral-700">
                    City
                  </label>
                  <div className="flex px-3 py-2 items-center border rounded-lg border-neutral-200 bg-white focus-within:border-orange-500 focus-within:ring-2 focus-within:ring-orange-500/10 transition-all duration-150">
                    <HiOutlineOfficeBuilding className="w-4 h-4 text-neutral-400 shrink-0" />
                    <input
                      type="text"
                      id="city"
                      value={formData.city}
                      onChange={handleInputChange}
                      placeholder="San Francisco"
                      className="flex-1 ml-2.5 outline-none bg-transparent text-xs text-neutral-900 placeholder:text-neutral-400"
                      required
                    />
                  </div>
                </div>

                <div className="md:col-span-2 space-y-1">
                  <label htmlFor="address" className="block text-xs font-medium text-neutral-700">
                    Street Address
                  </label>
                  <div className="flex px-3 py-2 items-center border rounded-lg border-neutral-200 bg-white focus-within:border-orange-500 focus-within:ring-2 focus-within:ring-orange-500/10 transition-all duration-150">
                    <input
                      type="text"
                      id="address"
                      value={formData.address}
                      onChange={handleInputChange}
                      placeholder="e.g. 123 Market St, Suite 400"
                      className="flex-1 outline-none bg-transparent text-xs text-neutral-900 placeholder:text-neutral-400"
                      required
                    />
                  </div>
                </div>
              </div>

              {/* Business Hours Text Area */}
              <div className="space-y-1">
                <label htmlFor="businessHours" className="block text-xs font-medium text-neutral-700">
                  Business Hours
                </label>
                <div className="flex px-3 py-2 items-start border rounded-lg border-neutral-200 bg-white focus-within:border-orange-500 focus-within:ring-2 focus-within:ring-orange-500/10 transition-all duration-150">
                  <HiOutlineClock className="w-4 h-4 text-neutral-400 shrink-0 mt-0.5" />
                  <textarea
                    id="businessHours"
                    value={formData.businessHours}
                    onChange={handleInputChange}
                    placeholder="e.g. Mon - Fri: 9:00 AM - 6:00 PM&#10;Sat: 10:00 AM - 4:00 PM"
                    rows={3}
                    className="flex-1 ml-2.5 outline-none bg-transparent text-xs text-neutral-900 placeholder:text-neutral-400 resize-none min-h-[70px]"
                    required
                  />
                </div>
              </div>
            </form>
          </div>

          {/* Stepper Footer Action */}
          <div className="mt-6 pt-4 border-t border-neutral-100 flex flex-row items-center justify-between gap-4">
            <button
              type="button"
              onClick={() => setStep((prev) => Math.max(1, prev - 1))}
              className="px-4 py-2 font-medium text-xs text-neutral-500 hover:text-neutral-800 transition-colors"
            >
              Back
            </button>
            <button
              type="submit"
              onClick={handleSubmit}
              className="px-5 py-2 font-medium text-xs bg-orange-500 hover:bg-orange-600 text-white rounded-lg shadow-sm hover:shadow active:scale-[0.98] transition-all duration-150"
            >
              Continue
            </button>
          </div>
        </div>

        {/* Right Panel: Globe Side */}
        <div className="hidden lg:flex lg:col-span-5 flex-col items-center justify-between bg-orange-500 p-8 relative overflow-hidden text-white">
          {/* Backdrop gradient shading */}
          <div className="absolute inset-0 bg-[radial-gradient(circle_at_center,rgba(255,255,255,0.15)_0%,transparent_75%)] z-0" />
          
          {/* Top Panel Text */}
          <div className="relative w-full z-10 space-y-1 text-center mt-2">
            <span className="text-[10px] tracking-wider uppercase font-bold text-orange-100 bg-white/10 px-2 py-0.5 rounded-full">
              Step 2 of 3
            </span>
            <h3 className="text-base font-bold pt-1">Global Scale Engine</h3>
            <p className="text-xs text-orange-100/80 max-w-50 mx-auto leading-normal">
              Visualize your market location live. We map your branches onto global telemetry charts seamlessly.
            </p>
          </div>

          {/* Center Graphic: Aceternity Globe Area */}
          <div className="relative w-full aspect-square flex items-center justify-center my-auto z-10 max-h-[260px]">
            <WorldGlobe />
          </div>

          {/* Bottom Badge */}
          <div className="relative w-full z-10 text-center border-t border-white/10 pt-3">
            <p className="text-[11px] font-medium text-orange-50/90 tracking-wide">
              Reliable • Fast • Confidential • Secure
            </p>
          </div>
        </div>

      </div>
    </motion.section>
  );
}

